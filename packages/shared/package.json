{
  "name": "@ecs/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, DTOs, and constants used across apps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
