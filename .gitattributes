# Shell scripts must stay LF even when checked out on Windows -- they run
# on Linux (EC2 user-data, container entrypoints, CI runners). A CRLF
# shebang line breaks the interpreter.
*.sh text eol=lf
